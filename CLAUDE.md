# Task Board Project

## プロジェクト概要

React + Vite で構築したタスク管理ボードアプリケーション。

## 技術スタック

- **フロントエンド**: React 19
- **ビルドツール**: Vite 8
- **言語**: JavaScript (JSX)

## 開発環境のセットアップ

```bash
# 依存関係のインストール
npm install

# 開発サーバーの起動（http://localhost:5173）
npm run dev

# プロダクションビルド
npm run build

# ビルド結果のプレビュー
npm run preview

# リント
npm run lint
```

## アプリの機能

- テキスト入力でタスクを追加（Enter キーまたは「追加」ボタン）
- チェックボックスで完了・未完了を切り替え
- タスクの削除
- 完了済みタスクはグレー表示 + 打ち消し線

## Git 運用ルール

### 基本方針

- **コードを変更するたびに GitHub へプッシュする**
- `main` ブランチへの直接プッシュは禁止。作業は必ずフィーチャーブランチで行う
- コミットメッセージは日本語または英語で、変更内容が分かるように記述する

### ブランチ命名規則

| 種別 | プレフィックス | 例 |
|------|--------------|-----|
| 機能追加 | `feature/` | `feature/add-task-filter` |
| バグ修正 | `fix/` | `fix/task-delete-error` |
| リファクタ | `refactor/` | `refactor/board-component` |
| ドキュメント | `docs/` | `docs/update-readme` |

### コミット & プッシュの手順

```bash
# 1. 変更をステージング
git add <変更ファイル>

# 2. コミット
git commit -m "変更内容の説明"

# 3. リモートへ即時プッシュ
git push origin <ブランチ名>
```

### プッシュのタイミング

- 機能の実装が完了したとき
- バグ修正が完了したとき
- 作業を中断・終了するとき
- レビューを依頼するとき

> コードの変更は常にリモートと同期し、ローカルにのみ存在する状態を避ける。

## コーディング規約

- コメントは原則として書かない（変数名・関数名で意図を表現する）
- 不要なエラーハンドリングや抽象化を追加しない
- セキュリティ上の問題（XSS 等）に注意する

## ディレクトリ構成

```
task-board/
├── public/           # 静的ファイル
├── src/
│   ├── App.jsx       # メインコンポーネント
│   ├── App.css       # コンポーネントスタイル
│   ├── index.css     # グローバルスタイル
│   └── main.jsx      # エントリーポイント
├── index.html
├── vite.config.js
└── CLAUDE.md         # このファイル
```
